[package]
name = "alera-core"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = []
# The async spawn helper, for crates that only need `child_process`.
async-process = ["dep:tokio"]
runtime = ["async-process", "dep:anyhow", "dep:chrono", "dep:chrono-tz", "dep:serde", "dep:serde_json", "dep:sqlx", "dep:thiserror", "dep:url"]

[dependencies]
anyhow = { version = "1", optional = true }
cap-fs-ext = "4.0.2"
cap-std = "4.0.2"
chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"], optional = true }
chrono-tz = { version = "0.10", optional = true }
git2 = { version = "0.21", default-features = false }
ignore = "0.4"
same-file = "1.0.6"
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }
sqlx = { version = "0.9", default-features = false, features = [
    "sqlite",
    "runtime-tokio",
    "tls-rustls",
    "chrono",
], optional = true }
thiserror = { version = "2", optional = true }
tokio = { version = "1", features = ["process"], optional = true }
url = { version = "2.5", optional = true }
uuid = { version = "1", features = ["v4", "serde"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
